name: mobilelab2cet168 # แก้เป็นชื่อโปรเจคของคุณ
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.2.1 # บรรทัดนี้จะถูกเพิ่มเข้ามาอัตโนมัติ

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # เพิ่มส่วนนี้เข้าไป
  assets:
    - assets/images/